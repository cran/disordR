## -----------------------------------------------------------------------------
library("disordR")
a <- rdis()
a

## -----------------------------------------------------------------------------
a^2
max(a)
sort(a)

## ----error=TRUE---------------------------------------------------------------
b <- rdis()
b
a+b

## -----------------------------------------------------------------------------
a[a<0.5] <- 0  # round down
a
b[b>0.6] <- b[b>0.6] + 3  # add 3 to every element greater than 0.6
b

## -----------------------------------------------------------------------------
a <- disord(1:10)
a
x <- a + a^2
x
a[a<5] <- x[a<5]
a

